import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.ibb.co.com",
      "images.unsplash.com",
      "cdn.i-scmp.com",
      "www.figma.com",
    ],
  },
};

export default nextConfig;
